.contents.row
  - @tweets.each do |tweet|
    .content_post{style: "background-image: url(#{tweet.image});"}
      = simple_format(tweet.text)
      %span.name
        %a{href: ""}
          %span 投稿者
          = tweet.user.nickname
  = paginate(@tweets)



